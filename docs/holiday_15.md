# Holiday_15 시작 문서

작성: 2026-09-10
기준 리비전: 6977e0e (main, #59 머지 커밋)
이전 세션: Holiday_14 문서 (#59–#63, 이슈 #64 신설)

코드에 없는 것만 담는다. 확장 네 피드의 규칙·근거·테스트는 rules/·tests/ 가
들고 있고, 이 세션의 조사 보고 여섯 벌(report_sh_gesetz_chain.md,
report_bw_gesetz_chain.md, report_ni_gesetz_chain.md, report_rp_gesetz_chain.md,
report_cron_obs_2.md, report_pr59_status.md)은 /tmp 에 있었다 — **다음 세션에서는
받을 수 없다(§1-4).** 작성 시점에 앞의 넷은 이미 없다. 재수령 절차는 각 보고서
§수신 절차에 있었고, 요약은 아래 §3 에 옮겼다.

---

## 0. 이 세션에서 한 일

PR 다섯, 이슈 하나.

**독일 주 피드 확장 4벌 — 완주.** 조사 넷 + 구현 넷이 한 세션에서 돌았다.
#60 de_sh(10건 전건 true) · #61 de_bw(12건 전건 true) · #62 de_ni(10건,
reformationstag 만 true) · #63 de_rp(11건 전건 false). 주 피드 5 → 9,
발행 피드 11 → 15, 테스트 963 → 1317. 순서는 계획대로 SH → BW → NI → RP.
각 구현 브랜치는 조사 세션의 개정 체인 마감을 선행 단계로 두었고, 스팟
재검증(요석 공포본 1건 재수령·sha256 대조)을 매 PR 에 넣었다.

**랜딩(#59) — 노출 공백 마감.** 구독 절을 feed-data 블록으로 축약하는 기존
PR 을 main 기준으로 갱신하고 신규 넷을 세웠다. 결속 테스트(표시명·설명이
rules/de_*/feed.py 에서 유도됨)와 정렬 테스트(아코디언 key 알파벳순)를
더해 1328 passed. 2026-09-09 부터 열려 있던 노출 공백(발행본 아홉, 랜딩
다섯)이 하루 만에 닫혔다.

**publish cron 관측 — 1/6 에 머묾.** 2026-09-08 21:23 UTC 예정분 실측
(run 34290682388, 지연 123.83분, startedAt == createdAt). 이후 관측을
잘못된 날짜로 시도해 빈 날을 조사했다(§1-1).

닫힌 결정(각 PR 의 본문·커밋에 상세):

- **verified true 신규 발행 전례 1호(SH).** 승격 경로(#56: false 로 발행 후
  관보 확보 시 승격)와 별개의 두 번째 경로 — 조사 세션에서 체인을 마감하고
  공포본 자구를 확보하면 처음부터 true 로 발행한다. BW 가 두 번째 사례.
- **인용 체인 마감을 verified 근거로 인정.** SH 2006~2015 구간(연도판 6.2GB
  미스캔), BW 중간 다섯 법(1972~1983 미수령)을 공포본 인용 체인으로 닫았다.
  BW 는 이중 근거 — 1995 Neufassung 인쇄 자구가 1970 자구에서 알려진 개정
  둘만으로 남김없이 유도된다(중간 개정이 § 1 을 건드렸다면 산식이 안 닫힌다).
- **key 는 개념 식별자, SUMMARY 가 조문 표기를 든다.** heilige_drei_koenige·
  fronleichnam·allerheiligen 을 de_by 에서 재사용하고, SUMMARY 는 주마다
  갈린다(BW "Fronleichnam" vs RP "Fronleichnamstag", HH "31. Oktober" vs
  SH "Reformationstag"). 조문이 다르면 표기가 다른 것이 정직하다.
  이 세션 신규 token 명명 0건.
- **10-03 의 근거는 조문 열거 유무가 가른다.** BW 는 § 1 밖이라 rules/de 를
  미러(Einigungsvertrag Art. 2 Abs. 2, verified true) — 주 피드 첫 사례.
  NI·RP·SH 는 조문 안이라 조문을 주근거로 두고 Einigungsvertrag 는
  DESCRIPTION 부기(HH 방식).
- **RP § 2 Abs. 1 Nr. 10 "der 1. und 2. Weihnachtstag" 분해.** 한 호 →
  두 항목, 둘이 같은 자구를 인용하고 병렬 분해를 서술. 이름 있는 두
  공휴일의 병렬이라 DTEND 하루 단위 규칙과 충돌하지 않는다.
- **랜딩 feed-data 블록은 사람이 쓴다.** 발행본이 생기면 랜딩이 따라오는
  구조가 아니라, 안 따라온 것을 CI 가 알려주는 구조다. PR 본문에 절 하나로
  명시했다(오해 방지).
- **표시 순서(알파벳순) ≠ 기계 순서(등록순).** 랜딩 아코디언은 key
  알파벳순, status.json 키는 rules/status.py 리터럴 등록순. 목적이 다르니
  일치시키지 않고, 각각을 테스트가 고정한다.
- **Holiday_07 §7 의 셋과 Holiday_08 §6 의 다섯 줄 규칙은 레포로 승격**(§6).
  8세션 유지된 규칙은 세션 문서가 아니라 레포 안에 둔다. 이 세션에서
  실행해 AGENTS.md 「조사와 보고」 로 옮겼다.

## 1. 정정

- **publish cron 발화일은 UTC 기준 일·화·목이다.** `23 21 * * 0,2,4` 의
  요일 필드는 UTC 요일이고, KST 수령일이 월·수·금이다. 세션 중 KST 요일을
  UTC 날짜에 그대로 붙여 2026-09-09(수) 예정분을 조사했으나 그 날짜는
  발화 대상이 아니었다 — 지연이 아니라 예약 없음. **관측은 1/6 에 머문다.**
  남은 관측 일정(UTC): 09-13 · 09-15 · 09-17 · 09-20.
- **de 계열 verified false 는 xfail 로 다루지 않는다.** #62 프롬프트가
  "false 항목이 들어오니 xfail 델타가 정상"을 전제했으나, 레포 실태는
  자동 xfail 이 kr fixture 전용이고 de 의 false 는 source_todo + 상태
  고정 테스트다(HH 가 이미 그 방식). 프롬프트의 선행 실측 조항이 오류를
  흡수해 결과는 무사했다. 이 세션 xfailed 4 · xpassed 29 불변.
- **결속 테스트는 추가 직후 빨개지지 않는다.** #59 프롬프트가 "테스트
  선행 → red 확인"을 전제했으나, 결속은 존재하는 줄만 검사하므로 아직
  없는 넷은 잡을 수 없다. 그 시점 빨간 것은 목록 일치 두 건이었다
  (2 failed, 8 passed). 안전망 선행의 목적(변경 전 계약 고정)은 그대로
  달성됐고 전제만 좁았다.
- **레포 밖 보고서(/tmp)는 세션을 넘지 못한다.** BW 조사에서 첨부로 지정한
  report_de_batch2.md 가 파일시스템에 없었다(ls 실패). holiday_14 §서두의
  "다음 세션 첨부는 각각 필요하다" 전제가 실측으로 반증됐다. 한 건의 부재로
  끝날 일이 아니었다 — 작성 시점 전수 확인 결과, 레포(rules/·docs/·tests/)가
  이름으로 참조하는 /tmp 보고서 **11개 전부**가 없다(실재 0개). 이 세션 여섯
  벌 중 넷도 이미 사라졌다. 이후 NI·RP 프롬프트는 첨부 없는 전제로 작성했고,
  독립 실측 규약이 공백을 메웠다.
- **"셋·다섯 줄 규칙"의 소생과 항목 수가 8세션 동안 표류했다.** 이전 작업의
  1단계 실측에서 드러났다. 다섯 줄 규칙은 holiday_08 §6 이 만든 것이고
  holiday_08 이 스스로 "이번 세션이 하나를 더했다" 라고 적었는데, holiday_09
  부터 "Holiday_07 §7 의 셋과 다섯 줄 규칙" 으로 묶이면서 07 §7 소생으로
  귀속됐다 — holiday_07 §7 에 다섯 줄 규칙은 없다. 항목 수도 §7 "셋 다" →
  §6 "넷 다" → holiday_09 수 언급 소멸 → holiday_12 "셋·다섯 줄" 압축으로
  읽히지 않게 됐다(지금 문면은 "셋 또는 다섯 줄" 로도 읽힌다). 내용 자체는
  표류하지 않았다. 문서 간 전파 오류의 세 번째 사례다 — holiday_12 "13건",
  holiday_14 "피드별 로그 원복" 에 이어. 이전 작업 안에서 한 번 더 재현됐다 —
  2단계 지시문이 원문의 "커밋 oneline" 을 둘로 읽어 다섯을 여섯으로 셌고
  (holiday_12 §1 "기존 여섯 줄" 오기와 같은 형태), 닫힌 결정("문면은 원문
  그대로")이 원문인 다섯을 채택하게 했다. 이전(§6)이 그 표류를 닫는다.
- **#59 는 신규 피드를 자동 노출하지 않는다.** 세션 내내 "랜딩은 #59 가
  자동으로 흡수한다"를 가설로 두고 확장 네 PR 에서 index.html 을 미뤘으나,
  실체는 index.html 안 수기 JSON 블록이었다. 확장 네 PR 은 index.html 을
  한 줄도 건드리지 않았고, 그것을 잡는 테스트가 #59 안에 있어 넷 다 조용히
  CI 를 통과했다. 결과적으로 발행본 아홉 중 다섯만 노출되는 상태가 하루
  지속됐다. "랜딩은 후속 PR 몫" 결정 자체는 마크업 반복 방지 목적으로
  맞았으나, 노출 공백 비용을 과소평가했다.
- (부수) 지연 표본은 cron 세대별로 갈라야 한다. 변경 커밋 b83bf09
  (2026-09-07T08:56:20Z)보다 앞선 run 은 구 cron(`0 0 * * 1`) 표본이다 —
  09-07 run(236.70분)이 여기 해당한다. 전수 실측으로 닫았다: schedule run 은
  지금까지 **6건이 전부**이고, 구 cron 4건이 98.78 · 101.17 · 292.05 ·
  236.70분, 신 cron 2건이 123.83 · 111.23분이다. 신 cron 2건은 현재 구 cron
  범위 안쪽이다 — 오프셋 효과 판정은 하지 않는다. holiday_14 의 "schedule
  4/4회 98~292분" 은 구 cron 표본 서술로서 **맞다**(cron 활성화 2026-08-11
  이후 월요일 4회가 전부 발화했고 4회 모두 늦었다). 전 6건 startedAt ==
  createdAt — run 생성 지연이지 실행 지연이 아니다.

## 2. 검토했다가 버린 갈래

- **`erscheinungsfest` 신규 token**: key 를 조문 자구로 가르면 같은 축일이
  주마다 다른 key 가 되고, 기존 발행 UID(de_by)는 영구라 소급 통일이
  불가능해 원칙이 반쪽으로 남는다 — 개념 key 재사용으로 기각.
- **BW 10-03 을 FTG 근거로 적기**: § 1 열거에 없다. 조문에 없는 항목에
  조문 근거를 다는 것이라 기각, rules/de 미러로 전환.
- **BW R1 다섯 법(1972~1983, 약 6.2GB) 전수 스캔**: 산식이 남김없이 닫혀
  추가 정보량이 사실상 0 — 미수령으로 확정, 필요 시 절차만 남겼다.
- **RP 2003 이전 공포본 추적**: gvbl.rlp.de 하한이 2004, dilibri 는 JS
  차단. 전건 false 확정으로 전환하고 source_todo 경계 서술로 마감.
- **랜딩 표시명의 자동 유도**: status.json 피드 항목에 표시명 필드가 없어
  (path·events·range·provisional_events 뿐) 런타임 fetch 로 바꿔도 이름은
  따로 적어야 한다 — 구조 변경 없이 결속 테스트로 대체.
- **noscript fallback**(#59 Codex ⑤ 제안): 목록이 두 곳이 되면 결속
  테스트도 이중화되어 마크업 반복을 없애려던 PR 취지와 상충 — 기각,
  사실 핵심만 이슈 #64 로 분리.
- **#59 를 먼저 머지하고 노출을 별도 PR 로 보충**: main 직푸시 금지·PR
  필수 규칙상 빨간 CI 를 남기는 경로 — 브랜치에서 고쳐 녹색으로 머지.

## 3. 레포 밖에서 확인된 것

관보 아카이브 축(주별로 구조가 전부 다르다):

- **SH — Verkündungsportal.** 연도판 PDF `…/mm/gvobl_jahrgang_<YYYY>/
  GVOBl_<YYYY>.pdf` (1947~2024, 1996 만 대문자 `GVOBL`, 2024 는 본편+목차
  분리). 2004 연도판 506MB·2016 은 889MB — `Accept-Ranges: bytes` 지원,
  이어받기 가능. 2025~ 는 호별 PDF 체제, 목록화는 검색 폼으로. robots.txt
  Crawl-delay 180.
- **BW — Landtag 사이트맵이 전수 목록.** `service-sitemap-ltbw.de-
  sitemap{1,2,3}.xml.gz` 에 GBl PDF 2,101건. URL 의 id·hash 에 규칙이 없어
  사이트맵 없이는 열거 불가. 호별 0.1~28MB 로 작아 Range 불필요.
  1994 Nr.25–29·2005 Nr.1·2007 Nr.21 은 텍스트층 없음(이미지 육안).
  2024~ 는 전자 관보가 정본 — `tx_solr[q]` 전문검색, 상세 페이지의
  토큰 URL 로 PDF.
- **NI — 포털 + 전자 관보 이원.** 2004~2023 은 niedersachsen.de 호별 PDF
  (연도 페이지 URL 에 규칙 없음, 목록 페이지에서 href 수집). 2024~ 는
  **verkuendung-niedersachsen.de 가 공식 sha256 을 게시한다** — `/pubStore`
  전 목록, `/api/ndsgvbl/<yyyy>/<nr>` 메타·해시, `POST /api/search` 전문검색
  (sheet 키는 `ndsgvbl`). 포털이 불안정할 때는 Wayback 사본과 sha256 대조로
  교차 확인할 수 있다.
- **RP — gvbl.rlp.de 2004~.** 2020년은 슬러그 `/online-archiv/2022-1` 아래에
  있다(연도와 슬러그가 어긋난다). verkuendung.rlp.de 는 2026 Nr.12 부터.
  2003 이전 무료 공개 없음.
- **통합본 포털은 대부분 juris 계열 JS 셸**(SH·BW·RP 확인). **NI-VORIS
  (Wolters Kluwer)만 정적 HTML** 이고 조문별 버전 이력 문서를 따로 들고
  있어 [2차] 근거로 쓸 수 있었다. schure.de 가 NI 보조.

그 외:

- **feiertage-api 의 `hinweis` 필드는 비공휴일 표시다.** BW 응답 13건 중
  Reformationstag 은 hinweis(§ 4 Abs. 3 schulfrei 설명)가 붙은 비공휴일.
  대조는 hinweis 공란 항목 기준으로 한다 — 소스의 초과 수록은 존재의
  증거가 아니다(KASI 2015-08-14 누락 사례의 거울상).
- 확장 네 주 모두 python-holidays 0.102 와 발행 범위 전 연도 일치.
- BW § 1a(2017 한시 Reformationsfest)·SH 2017 일회성 LVO·RP 2015 LVO
  (2017분)는 전부 발행 하한(2020) 밖 — 무행동.

## 4. Codex 리뷰의 자리

다섯 PR 전부 실행(전부 신규 load 경로 또는 사용자 노출면), 생략 0건.
확인 항목 ①~⑤ 를 매번 PR 본문에 명시했다.

- **#60 지적 1건 [medium]** — 성탄절 두 항목의 2018 재번호 인용부에 공포일
  누락(#56 서지 형식 미달). [남김] 판정 후 테스트 강화(93972b3) → 데이터
  수정(3272ef9) → 재생성(2f7c125) 순으로 반영. 강화된 테스트가 데이터 수정
  전에 실제로 빨간 것을 확인했다.
- **#61·#62·#63** — approve, 지적 0건.
- **#59 ③ 수용** — 알파벳순이 계약인데 강제되지 않음. 정렬 테스트를 더하고
  de_bw 를 배열 끝으로 옮겨 red 를 재현한 뒤 원복했다("수용에도 재현").
- **#59 ⑤ [뺌]** — JS-off 시 구독 절이 빈다는 [high] 지적. 전제(fallback
  링크가 원래 있었다)는 반증했으나 — main 의 정적 마크업도 URL 상자가 비어
  있고 webcal 은 `href="#"` 였다 — 사실 핵심(라벨·설명 가시성)은 인정해
  이슈 #64 로 분리. 판단 이력을 레포 안에 남기는 것이 /tmp 휘발 경험의
  반영이다.

## 5. 미결

- **2026-09-10T21:23Z(= 09-11 06:23 KST) run — 실측으로 좁힘.** 관측
  2/6 확보: run 34541229466, createdAt 2026-09-10T23:14:14Z, 지연 111.23분,
  startedAt == createdAt, success. 신규 네 피드(de_sh·de_bw·de_ni·de_rp)
  첫 자동 발행 확인 — 근거는 run 이 돈 시점(383417d)의 FEEDS 가 넷을 포함한
  15피드라는 것 + job success. 재생성 전후 불변 확인 완료 — 다만 유도다.
  run 커밋 f336af2 에 feeds/ 가 없다는 사실 단독으로는 불변과 미생성이
  갈리지 않는다(커밋 스텝의 피드 반복문은 `if [ -f "$FEED" ]` 안에서만
  비교하므로 파일 부재 시에도 스테이징 없이 넘어간다). 둘을 가르는 전제는
  피드 생성 스텝의 성공(job success)이 15개 파일의 존재를 보장한다는 것이고,
  그 위에서 커밋 스텝의 배타 분기(DTSTAMP 외 차이가 있으면 스테이징, 없으면
  원복)가 "feeds/ 없음 = 15피드 전부 DTSTAMP 외 불변" 을 준다. status.json
  은 generated_at 외에 kasi_key.days_left 가 699→698 로 함께 줄었다.
  남는 것은 지연 폭 재판정 하나 — 관측 6건이 모이는 2026-09-20T21:23Z
  발화 이후. §0·§1 의 "1/6" 은 세션 당시 서술이고, 현재 관측 수는 이 절이
  든다.
- **2차 배치 조사(BB·HB·MV·SL·SN·ST·TH)** — 관보 아카이브 축 포함.
  이 세션에서 넷을 돌아보니 주마다 구조가 전부 달랐다.
- **승격 백로그 재지형**: NW 11(recht.nrw.de — 최우선), BE 10(커버 하한 확인
  후), HE 10(관보 미조사 — 유일한 공백), HH 9(오프라인만), **NI 9(Nds. GVBl.
  1995 Nr.5·2002 Nr.3 실물 — Landtagsbibliothek·GWLB 또는 juris 유료)**,
  **RP 11(2003 이전 부재 확정 — 실물 외 경로 없음)**. 주 피드 아홉의
  verified 현황은 true 36 / false 60(SH 10·BW 12·BY 12 가 전건 true,
  HH·NI 의 reformationstag 각 1건이 나머지).
- **BGBl. 1990 II S. 889 공포본 확보** — Einigungsvertrag Art. 2 Abs. 2.
  현재 rules/de 와 de_bw 가 gesetze-im-internet 통합본을 근거로 들고 있어
  #56 서지 형식(공포본 PDF + sha256)에 못 미친다. 10-03 을 든 전 피드에
  일괄 해당.
- **DESIGN.md 에 피드 추가 규약이 없다.** README 2차가 드러냈다 — 개정 전
  README 는 "국가를 추가할 때 반드시 늘리는 것은 sources/<코드>/ 와
  rules/<코드>/" 라고 적고 있었으나 독일 10피드는 sources/ 없이 서 있었다.
  README 는 "rules/ 는 필수, sources/ 는 갱신되는 외부 원천이 있을 때만"
  으로 고쳤는데, DESIGN.md 에는 그 축이 없다 — 절이 셋(kr/seasonal,
  SEQUENCE, 발행 파이프라인)뿐이고 피드 추가 규약 절 자체가 없다(실측).
  설계 결정의 정본이 DESIGN.md 이므로 축이 README 에만 사는 것은 자리가
  틀렸다. **— 2026-09-11 닫힘.** #69 가 DESIGN.md 에 결정 절을 세우고,
  #70 이 docs/operations.md 에 절차를 세웠다(§7).
- **독일 verified 가 status.json 에 집계되지 않는다.** verification 은
  집계 대상 표 넷이 전부 rules/kr/ 의 것이고(solar·lunar·designated·
  substitute), feeds.de_* 항목은 path·events·range·provisional_events
  뿐이다. 주 피드 아홉의 true 36 / false 60 을 밖에서 볼 방법이 지금은
  없다. README 2차에서 둘이 다른 것임을 문장으로 갈라 두었으므로(#66),
  집계를 신설한다면 이름을 어떻게 가를지가 함께 정해져야 한다.
- **랜딩 다국어(일본어·영어)** — 외부 수요 확인됨, 긴급하지 않음. 착수
  순서는 이슈 #64 뒤(JS-off 설계가 겹친다). 착수 전에 닫아야 할 것 셋.
  (1) 번역문 정본 위치 — 결속 테스트가 랜딩 label·desc 를 rules/de_*/feed.py
  에서 유도되는지 검사하므로, 언어축이 어디 사는지가 테스트 구조를 가른다.
  (2) 전환 방식 — JS 스위치 대 언어별 정적 경로. 전자는 #64 공백을 3배로
  만든다. (3) 번역 범위 — UI 문구만인가, 피드 표시명·주 이름까지인가.
  랜딩 표시명은 데이터가 아니라 UI 이므로 출처 규칙 밖이다. 층이 둘이다 —
  랜딩(되돌리기 쉬움)과 피드 내용(CALNAME·DESCRIPTION 이 한국어, 바꾸면
  SEQUENCE 상승이 전 구독자에게 나간다)은 비용이 다르며, 후자는 이번 범위
  밖이다.
- **(2026-09-11 추가) tests/ 안 피드 리터럴이 무방비다.** test_published_feed
  의 피드당 3함수와 UID 배타 튜플, test_de_scope 의 STATE_FEEDS·LAND_NAMES.
  새 피드를 여기 안 더하면 그 피드가 재현성·UID 배타·scope 검사에서 빠지고
  CI 는 녹색이다. 집합 일치 테스트(#68)로는 잡히지 않는다. `rules/` 스캔
  기반 parametrize 리팩터가 답으로 보이나 덩치가 달라 범위 밖이었다.
  **— 같은 날 범위 축소.** test_published_feed 쪽은 #72 로 닫혔다(rules/
  스캔 parametrize, §7). 남은 것: **test_de_scope 의 STATE_FEEDS·LAND_NAMES.**
  #72 가 남긴 것 하나 더 — `provisional == 0` 의 jp + de 10 리터럴 목록.
  접두사 판별(`startswith("de")`)을 쓰지 않기로 한 결과라 새 피드 추가 시
  갱신 대상으로 남는다.
- **(2026-09-11 추가) README 구조 절(산문)도 무방비다.** 기계 추출이 불가해
  테스트로 고정할 수 없다. #70 체크리스트에서 사람 몫으로 적었다.
- **(2026-09-11 추가) 로컬 pytest 녹색이 CI 녹색을 뜻하지 않는다.** #68 에서
  docstring 한 줄이 E501 로 CI 에서만 걸렸다. lint 가 로컬 테스트 경로에
  붙어 있지 않다.
- **(2026-09-11 추가) Codex 리뷰 이력이 레포에 남지 않는다.** 리뷰는 로컬
  companion 에서 돌아 GitHub 에 코멘트·리뷰로 남지 않는다. 이날 세 번(#68
  첫·재, #72) 돌렸으나 레포에 남은 것은 #68 본문의 "지적과 수용" 절 하나뿐이고
  그것도 지시로 들어간 것이다. 이 레포는 git 히스토리를 감사 로그로 삼는데
  리뷰 이력만 세션에 갇힌다. 해결 갈래는 여럿 — companion 이 PR 코멘트를
  남기게 / PR 본문 결과 절 의무화 / 세션 문서에 리뷰 로그 절. 이번에 정하지
  않는다.
- 이슈 **#64**(JS-off 구독 절), **#40**(독일 피드 검토 — 기존).
- README 2차 — 운영자 전용 절의 docs/operations.md 분리는 #66 에서
  실행됐다. 실기기 스크린샷과 "발표는 어디서" 방 문답 반영은 미결로
  남는다. 그 밖에 verified 기준 확장 (b)안 보류(실물 열람 경로 부재),
  방·Issues 회신 대기, attribution 판정 보류, 이월 잔여(holiday_13 §5).

## 6. 규약

- **승격: CC 독립 재실측.** 4세션 누적, 이 세션에서 세 차례 더 — cron
  관측 1/6 에서 프롬프트의 예상값(23:27:32)을 받아 적지 않고 실측해
  실제 createdAt(23:26:50)을 잡았고(받아 적었으면 틀린 값이 기록됐다),
  BW 에서 첨부 부재 상황을 전 항목 독립 실측으로 메웠고, #59 에서
  보고서의 label·desc 값 대신 feed.py 에서 직접 유도했다.
- **승격: 검증 스크립트 실패 시 push 중단 배선.** `set -euo pipefail` +
  요약 줄 정확 일치 단언. 이 세션에서 두 차례 실제로 멈췄다(#60 xfailed
  부분 문자열 오검출, #61 단언 과잉 범위) — 둘 다 "멈춤 → 원인 판별 →
  정밀화 → 재개"로 흘렀고, #56 의 `set -e` 무정지 통과와 대비가 완성됐다.
- **신규 후보(1세션): 사람의 설계 전제도 레포 실측 앞에서는 이차 소스다.**
  이 세션에서 세 번 뒤집혔다 — xfail 델타(§1-2), cron 발화 요일(§1-1),
  결속 테스트 red 예상(§1-3). 세 번 다 프롬프트의 **선행 실측 조항**이
  오류를 흡수했다. "CC 보고는 이차 소스"의 대칭이며, 조치는 같다 —
  판단을 가르는 전제는 프롬프트에 실측 지시로 박는다.
- **신규 후보(1세션): 레포 밖 산출물은 세션을 넘지 못한다.** 한 건의 사고가
  아니라 규칙이다 — 레포가 이름으로 참조하는 /tmp 보고서 11개를 전수 확인해
  전부 부재였다(§1-4). 보고서 첨부를 전제한 프롬프트를 쓰지 않는다. 필요한
  사실은 세션 문서(§3)나 레포 안에 옮겨 두고, 재수령 절차만 남긴다.
- 유지: 테스트 커밋 선행(#59·#60·#61·#62·#63 전부), 조사 프롬프트와 구현
  프롬프트 분리, 멈춤 조건 명시(발동 0회 — 다섯 PR 모두 미발동), Codex
  항목 명시·수용에도 재현, [남김]/[뺌]/[대체], [추론]·[2차] 분리, 원본
  병기, grep 0 확인, 브랜치 세션 말 일괄 청소.
- **신규(2026-09-11): Codex 리뷰를 돌렸으면 결과를 보고한다** — 지적 유무,
  있었다면 내용과 판정. #72 에서 돌렸으나 보고가 없어 돌렸는지를 되물어야
  했다. 프롬프트가 보고 의무를 명시하지 않은 것이 원인이라 규약으로 세운다.
- Holiday_07 §7 의 셋과 Holiday_08 §6 의 다섯 줄 규칙 — **이전 완료.**
  8세션 만에 AGENTS.md 「조사와 보고」 로 옮겼다. 정본은 이제 그 절이고,
  다음 세션 문서에 이 항목을 다시 적지 않는다.

## 7. 이 문서 이후 — 2026-09-11 후속

같은 날 세션 문서(#67) 뒤에 이어진 작업이다. §0~§6 은 당시 서술을 그대로
둔다.

**한 일**

1. **PR #68 — 피드 집합 정합 테스트** (54f3b51). `tests/test_feed_set.py`.
   네 지점(rules/ · publish.yml FEEDS · rules/status.py · feeds/)이 같은
   피드 코드 집합을 드는지를 **FEEDS 를 허브**로 양방향 비교한다. 마커 없음
   2건(rules/ · status.py) + `published_artifact` 1건(feeds/).
   - **허브를 처음 feeds/ 로 잡았다가 Codex P1 을 받아 FEEDS 로 옮겼다.**
     지적: feeds/ 허브를 마커 없이 두면 "입력은 머지됐고 발행본은 아직 없는"
     상태에서 발행 run 의 테스트 스텝이 생성 스텝보다 먼저 실패해 첫 발행이
     막힌다. 수용 근거는 실측 — feeds/ 15개를 전부 치운 상태에서
     `-m "not published_artifact"` 가 1272 passed.
   - red 재현 셋(rules/ 고아 · status.py 등록 제거 · feeds/ 파일 제거), 마커
     분리 2/3. 재리뷰 지적 없음(정적 분석 한정).
   - 다음 예약 run(2026-09-13T21:23Z)부터 마커 없는 2건이 발행 run 에서 돈다.
2. **PR #69 — DESIGN.md** (5d1cac2). 피드 추가 결정 절 신설(sources/ 조건부 ·
   랜딩은 자동으로 따라오지 않음 · 표시 순서≠기계 순서 · 단일 공급원은
   FEEDS), L150 표의 인라인 15피드 열거 제거, DESIGN→README 역참조 0→1.
3. **PR #70 — docs/operations.md** (53fded1). 피드 추가 절차 체크리스트 열
   항목. 층 셋 — 발행 run 과 CI 가 모두 잡는 것 / CI 만 잡는 것(마커) /
   아무것도 잡지 않는 것(사람 몫) — 과 커밋 전 lint 항목.
4. **무방비 지점의 변화.** 조사 시점 다섯 → 현재 둘.
   - publish.yml FEEDS — #68 (허브가 됐다)
   - rules/ 고아 패키지 — #68 `test_every_rules_package_is_in_feeds_and_vice_versa`
   - DESIGN.md L150 열거 — #69 (열거 자체를 걷었다)
   - 남은 둘: README 구조 절(산문) · tests/ 리터럴 — §5 에 2026-09-11 추가
     항목으로 있다.
5. **PR #72 — test_published_feed 리팩터** (5cb4e93). 966→339줄. 피드당
   수작업 복제 함수를 rules/ 스캔 parametrize 로 — 재현성·status 서술·UID
   배타 각 15. jp 차이(build 의 today 유무, range 상수)는 리터럴 없이
   `inspect.signature`·`hasattr(feed_range)` 로 모듈이 말한다. 수집 46→58:
   −1(kr events>0 흡수) +10(de 계열 provisional==0 독립 케이스) +3(kr·jp·kr_jp
   UID 배타 신설 — 예외는 (kr, jp) 한 쌍, kr_jp 는 검사 대상). 검증 넷 —
   건수 증감 전부 설명, red 재현 넷(재현성·status·UID 메시지·kr_jp 편입),
   더미 피드 zz_dummy 로 수집 58→61 자동 편입, 원복 후 1346 passed.
   Codex 리뷰 지적 없음(정적 분석 한정). 리뷰 결과는 PR 에 코멘트로 남기지
   않고 본문 절로 대체했다 — 사후 코멘트는 타임스탬프가 머지 뒤라 리뷰
   시점을 흐린다. 남은 것은 §5 tests/ 리터럴 항목의 축소분.

**남은 미결** — §5. 여기서 반복하지 않는다.

**다음 큐** — 이슈 #64(JS-off 구독 절) → 랜딩 다국어(수요 확인됨, #64 뒤)
→ BGBl. 1990 II S. 889 공포본 확보 → 승격 백로그 → 2차 배치 조사 7개 주.
2차 배치가 큐 뒤인 것은 발행 중인 피드의 근거 품질이 확장보다 먼저이기
때문이다.
