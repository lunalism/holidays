# 실제 키는 여기에 쓰지 말 것. .env 로 복사해서 로컬에서만 사용한다.
# 운영 환경에서는 GitHub Actions Secret 으로만 주입한다.

# 공공데이터포털 서비스 키 (대한민국) — 한국천문연구원 특일 정보
# 이름은 sources/kr/kasi_client.py 의 ENV_VAR 과 같아야 한다. 어긋나면
# 예시대로 채워도 MissingServiceKey 가 난다.
KASI_SERVICE_KEY=
