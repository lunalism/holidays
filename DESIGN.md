# 설계 노트

구현 코드와 데이터 표에 담기 어려운 결정을 적어 둔다.
규칙 하나에 붙는 결정은 해당 YAML 의 주석과 `open_questions` 에 있다.
여기 오는 것은 피드 단위 범위 결정처럼 어느 파일에도 속하지 않는 것들이다.

---

## kr/seasonal — 24절기 피드는 계산하지 않고 KASI API 를 쓴다

**상태: 미구현.** v1 범위를 확정한 뒤에 착수한다. 아래는 착수할 때의 전제다.

### 결정

24절기 날짜는 한국천문연구원 24절기 API 로 받는다.
`rules/kr/astro.py` 의 태양 황경 계산을 쓰지 않는다.

### 이유 1 — 우리 정밀도로 절기 날짜를 보증할 수 없다

태양 황경은 Meeus 제25장 저정밀도 식으로 구한다. 문헌 오차 약 0.01°,
시간으로 약 15 분이다. 절기는 "황경이 15°의 배수가 되는 순간이 든 날"이므로,
그 순간이 KST 자정에서 15 분 안쪽이면 우리는 날짜를 정하지 못한다.

실측하면 드문 일이 아니다.

```
2015~2050 24절기 864 개 중 KST 자정 15 분 이내 19 개 (2.2%)

    0.44 분  2025-12-22  동지
    2.06 분  2030-02-18  우수
    2.09 분  2049-04-20  곡우
    2.56 분  2023-06-22  하지
    3.86 분  2021-02-03  입춘
      ...
```

2025 년 동지가 자정에서 **0.44 분**이다. 그 동지가 12 월 21 일인지 22 일인지
우리 계산은 답하지 못한다. 답하는 척할 수는 있지만 그건 근거 없는 값이다.

### 이유 2 — 공휴일에서는 흡수되지만 절기 피드에서는 흡수될 데가 없다

같은 오차가 공휴일 쪽에서는 문제가 되지 않는다. 거기서 절기(정확히는 중기)는
**중간 계산**이기 때문이다. 중기는 무중치윤법에서 "이 달에 중기가 들어 있나"를
판정하는 데만 쓰이고, 그 판정이 뒤집히려면 중기가 자정에 가까운 것만으로는
부족하고 그 자정이 마침 삭망월 경계여야 한다.

실제로 흡수된다는 것이 확인되어 있다. 태양 황경을 ±0.1° 흔들어도
2015~2050 년의 설날·추석·부처님오신날이 하나도 바뀌지 않는다(문헌 오차의 10 배).
`tests/test_lunar.py` 의 섭동 테스트가 이것을 상시 확인한다.

절기 피드는 다르다. 절기 날짜 **자체가 산출물**이다. 15 분 오차가 곧바로
구독자 캘린더의 하루 오차가 되고, 흡수될 중간 단계가 없다.

같은 계산을 한쪽에서는 쓰고 한쪽에서는 안 쓰는 것이 일관성 없어 보이지만
그 반대다. 오차가 결과에 닿는지 아닌지가 다르고, 그 차이가 판단을 가른다.

### 이유 3 — 절기는 관측값이라 발표가 정답이다

공휴일은 법령이 정하고 우리는 그 법령을 해석해 유도한다. 그래서 KASI 는
대조 상대이지 정답이 아니다. 실제로 2015-08-14 임시공휴일은 KASI 응답에
없지만 실재했다.

절기는 그렇지 않다. 절기는 해석의 대상이 아니라 천체의 위치이고, 한국에서
그 값을 확정해 발표하는 곳이 한국천문연구원이다. 우리 계산이 발표와 갈리면
발표가 맞다. 대조해서 우리 쪽 손을 들어 줄 여지가 없다.

즉 이 피드에서는 API 가 2차 소스가 아니라 1차 소스다.
`rules/kr/lunar_holidays.yaml` 의 축(계산 1차, 발표 대조)을 그대로 가져오면
안 되는 이유가 이것이다.

### 착수 전에 확인할 것

- 24절기 API 의 오퍼레이션명과 응답 구조 (`SpcdeInfoService` 계열로 보이나 미확인)
- 특일정보와 **별도 활용신청**이 필요한지 (키를 다시 받아야 하는지)
- 지원 연도 범위. 피드를 몇 년치 선발행할 수 있는지가 여기서 정해진다
- 응답이 없는 구간의 처리. 계산으로 메우지 말 것 — 그러면 이 결정이 무의미해진다

### 착수하면 지킬 것

- `rules/kr/astro.py` 를 절기 피드에서 import 하지 말 것.
  쓸 수 있게 두면 언젠가 "API 가 없는 해만 계산으로" 가 들어오고,
  그 순간 어느 값이 어디서 왔는지 구분되지 않는다
- 응답을 `sources/kr/cache/` 에 커밋할 것. 공휴일 쪽과 같은 규약이다
- coverage 축은 `last_synced_at` 이다. 규칙이 없고 받아 적는 표이기 때문이다.
  `rules/kr/substitute_rules.py` 의 `Coverage` docstring 참조

---

## kr 피드 — SEQUENCE 정책

**상태: 구현됨.** `core/ics.py` 의 `_sequences()` 와 `rules/kr/feed.py` 의
`publish()` 가 이 결정을 담고 있다.

SEQUENCE 의 진실 공급원은 `feeds/` 의 직전 발행본이다. 상태 파일을 따로 두지
않는다 — 두면 파일과 발행본이 어긋날 수 있고, 어긋났을 때 어느 쪽이 맞는지
정할 근거가 없다. 발행본이 사실이다.

### 미해결

- id: seq-증가-도달불가

  UID 가 `{YYYYMMDD}-{token}` 이라 날짜가 UID 에 들어 있다. 그래서 날짜 변경은
  같은 UID 의 DTSTART 가 바뀌는 것으로 나타나지 않는다. 이전 UID 가 사라지고
  새 UID 가 생기며, 그 시점에 "사라진 UID" 검사에 걸려 빌드가 중단된다.

  **따라서 현재 설계에서 SEQUENCE 는 항상 0 이다.** `_sequences()` 의 +1 규칙은
  지금 도달하지 않는다. 감소 방어도 같은 이유로 도달하지 않는다.

  +1 규칙을 지우지 않은 이유는 UID 규칙이 바뀔 경우를 위해서다. 지우면 그때
  같은 판단을 처음부터 다시 해야 하고, 남겨 두면 UID 에서 날짜가 빠지는 순간
  그대로 살아난다. 다만 "지금 작동하고 있다"고 읽으면 안 된다.

  실질적 영향: 음력 환산이 정정되어 설날이 하루 옮겨가면 발행이 멈춘다.
  조용히 틀리는 것보다 낫지만, 그때 사람이 결정해야 할 것이 둘 있다.

    - 사라지는 UID 를 STATUS:CANCELLED 로 내보낼 것인가
    - UID 에서 날짜를 뺄 것인가 (첫 발행 전까지만 가능하다)

  아직 발행한 적이 없으므로 두 번째 선택지가 열려 있다. 첫 발행 이후에는
  닫힌다. `tests/test_ics.py` 의
  `test_moving_a_date_shows_up_as_a_dropped_uid` 가 현재 동작을 고정한다.

- id: seq-내용변경-정책

  지금은 DTSTART/DTEND 가 바뀔 때만 SEQUENCE 를 올린다. SUMMARY·DESCRIPTION·
  STATUS 변경으로는 올리지 않는다. 표기나 우리 확신도가 달라진 것이지 일정이
  달라진 것이 아니고, 구독자가 다시 알림을 받을 일이 아니라고 보았다.

  반론이 있다. SEQUENCE 는 같은 UID 이벤트의 개정 순서를 뜻하므로, 내용이
  바뀐 발행본을 같은 번호로 내면 클라이언트가 어느 쪽이 최신인지 가릴 근거가
  없다. 특히 STATUS 가 TENTATIVE 에서 확정으로 바뀌는 것은 상태 변경이다.

  지금 UID 설계에서는 STATUS 전환이 실제로 일어나는 거의 유일한 변경이라
  (날짜 변경은 위 항목대로 빌드가 멈춘다) 실질 영향이 있다.

  판단 근거가 부족하다. 클라이언트마다 SEQUENCE 를 어떻게 쓰는지가 갈리고,
  우리는 아직 실제 구독으로 확인한 적이 없다. **실제 구독 테스트 후 재검토한다.**
  최소한 Google Calendar · Apple Calendar · Outlook 에서 같은 UID 의 내용만
  바꿔 재발행했을 때 무엇이 갱신되는지 보고 정할 것.

---

## 발행 파이프라인 — 세 파일의 성격이 다르다

`.github/workflows/publish.yml` 이 쓰는 산출물은 세 종류다. 셋 다 기계가
쓰지만 성격과 갱신 주기가 다르고, 섞으면 각자의 쓸모가 죽는다.

| 파일 | 무엇인가 | 언제 갱신되나 | 변화 없으면 |
|---|---|---|---|
| `feeds/*.ics` (목록은 `publish.yml` 의 `FEEDS` — 아래 "피드 추가" 절) | 구독자가 실제로 받는 것 | 내용이 실제로 바뀔 때만 | 원복. 커밋하지 않는다 |
| `status.json` | 지금 저장소가 주장하는 상태 | 매 실행 | 그래도 커밋한다 |
| `logs/build.jsonl` | 발행 시도 기록 | 매 실행 (실패 포함) | 해당 없음 — 늘 한 줄 는다 |

### feeds/*.ics — 변화가 있을 때만

나라마다 따로 비교한다. DTSTAMP 를 뺀 나머지가 같으면 그 피드는 원복하고
커밋하지 않는다. DTSTAMP 는 실행 시각이라 내용이 하나도 안 바뀐 재발행에도
매번 달라진다. 그대로 커밋하면 diff 가 늘 떠서 "무엇이 실제로 바뀌었나"를
diff 로 볼 수 없다. jp 편입 직후의 무변경 재발행(실행 33139146333)에서 두
피드 다 이 경로를 탔다.

공휴일 데이터는 몇 달씩 안 바뀌는 것이 정상이다. 이 파일들에 커밋이 없는
것은 문제가 아니라 정상 상태다.

### status.json — 변화가 없어도 매번

두 가지 때문에 항상 커밋한다.

**하나. 갱신이 끊긴 것을 밖에서 알 수 있어야 한다.**
이 파이프라인의 실패는 조용하다. 발행이 실패해도 이미 나가 있는 `.ics` 는
그대로 남아서 구독자 쪽에서는 아무 일도 없어 보인다. `generated_at` 이
밀리지 않으면 그것이 신호다. 피드만 보면 알 수 없고 이 파일이 있어야 보인다.

**둘. Actions 의 60 일 비활성화를 피한다.**
GitHub Actions 는 저장소에 60 일간 활동이 없으면 `schedule` 트리거를 자동으로
끈다. 공휴일이 몇 달 안 바뀌는 것이 정상인 이 저장소에서는, 피드에 변화가
없다는 이유로 커밋이 끊기면 예약 실행이 조용히 멈춘다. 멈춘 것도 조용해서
아무도 모르고, 그 사이 새로 지정된 임시공휴일이 피드에 안 들어간다.

`status.json` 은 `generated_at` 이 매번 바뀌므로 늘 커밋거리가 된다.
그 커밋 하나가 저장소를 살아 있게 유지한다. 부수 효과가 아니라 의도한 것이다.

### logs/build.jsonl — 실패도 남는다

성공만 적으면 "마지막 성공" 이후에 무슨 일이 있었는지가 남지 않는다.
무엇이 언제부터 안 되는지를 이 파일 하나로 읽을 수 있어야 한다.

워크플로의 기록 스텝은 `if: always()` 라 앞 스텝이 어디서 실패하든 돈다.
`result` 는 `job.status` 를 그대로 받고, 실패면 빌드 로그 끝부분을 `error` 에
싣는다. 2 회 연속 실패하면 이슈가 열린다 — 같은 제목의 열린 이슈가 있으면
새로 만들지 않는다. 매주 이슈가 하나씩 늘면 그 자체가 소음이 되어 아무도
안 보게 된다.

JSONL 인 것은 append 만 하면 되고 병합 충돌이 줄 단위로 끝나서다. 하나의
JSON 배열이면 매번 전체를 다시 써야 하고 두 실행이 겹치면 파일이 통째로 깨진다.

### 자동 커밋이 데이터 근거를 대체하지 않는다

워크플로는 위 세 파일만 쓴다. `rules/` 의 YAML 은 건드리지 않는다.

공휴일 규칙과 지정 내역은 사람이 관보·법령 원문을 확인하고 근거와 함께
커밋한다. 기계가 데이터를 고치기 시작하면 어느 값이 어디서 왔는지 구분되지
않고, 이 저장소가 근거를 들고 있다는 주장 자체가 무너진다.

KASI 응답에서 새 임시공휴일이 발견되어도 워크플로가 표에 넣지 않는다.
그건 `tests/test_source_agreement.py` 가 불일치로 보고하고, 사람이 근거를
확인해 `designated_holidays.yaml` 에 넣는다. 2024-10-01 이 그렇게 들어왔다.

### 발행 가드

`core/ics.py` 의 `UID_DOMAIN_CONFIRMED` 가 `False` 인 동안 `publish()` 가
거부한다. UID 네임스페이스가 잠정인 채로 나가는 것을 막는 장치였고, 지금은
`holidays.lunalism.com` 으로 확정해 열려 있다.

확정 후에도 가드를 남긴다. 다음 국가 피드가 붙거나 네임스페이스를 다시
손대야 할 때 같은 자리가 다시 쓰인다.

`build()` 와 `render()` 는 막지 않는다. 테스트와 검토는 계속 돌아야 하고,
막아야 하는 것은 만드는 것이 아니라 밖으로 나가는 것이다.

우회 인자를 두지 않았다. 두면 CI 가 그 인자를 쓰게 되고 플래그는 장식이 된다.
확정되면 그 값을 `True` 로 바꾸는 커밋 하나가 발행을 여는 것이고, 그 커밋이
곧 "도메인을 확정했다"는 기록이 된다.

---

## 피드 추가 — 무엇이 자동이고 무엇이 사람 몫인가

피드를 하나 늘릴 때 따라오는 결정 넷이다. 절차(어느 파일을 어떤 순서로
고치는지)는 여기 두지 않는다 — 여기 오는 것은 왜 그렇게 갈랐는가다.

### sources/ 는 조건부다

`rules/<코드>/` 는 필수다. `sources/<코드>/` 는 외부에서 갱신되는 원천이
있을 때만 둔다(kr: KASI API, jp: 内閣府 CSV). 독일은 근거가 관보 공포본이라
수집 대상이 아니고, 서지는 각 규칙 항목의 `source` 필드에 있다.
`data/<코드>/` 도 필수가 아니라 그 나라의 소스 사정에 따른다. `core/` 는
국가 중립으로 유지한다.

README 의 "구조" 절이 같은 사실을 디렉터리 서술로 들고 있다. 역할이 다르다
— 저쪽은 무엇이 어디 있는가, 여기는 왜 그렇게 갈랐는가. 결정의 정본은
여기다.

### 랜딩은 산출물이다 — 사람이 쓰는 것은 layout 과 locale 이다

`index.html` 은 `landing/render.py` 가 만든다. 구독 절의 feed-data 블록은
`landing/layout.yaml`(그룹 소속·순서, 언어 무관)과 `landing/locales/`(문구,
언어별), 그리고 `rules/` 스캔에서 생성된다. 발행본이 생기면 랜딩이 따라온다
— 발행 run 이 매번 다시 만들고, 바뀌었을 때만 올린다.

사람이 쓰는 것은 layout 과 locale 이지 index.html 이 아니다. 주 피드(de_*)는
layout 의 접두사 규칙으로 자동 편입되고, 그 밖의 새 피드는 어느 묶음에 설지를
사람이 layout 에 적는다.

두 가지 어긋남은 다르게 다룬다. **layout 에 자리가 없는 피드**는 render 가
ValueError 로 죽는다 — 발행 run 의 랜딩 생성 스텝이 실패해 발행이 멈춘다.
자리를 정하지 않은 것은 사람이 결정을 빠뜨린 것이라 조용히 넘기지 않는다.
반면 **커밋된 index.html 이 낡은 것**(layout·locale 을 고치고 다시 만들지
않음)은 발행을 막지 않는다 — 랜딩이 뒤처진 것이지 피드가 틀린 것이 아니고,
발행 run 이 스스로 다시 만들어 올린다. 그 검사(tests/test_landing_render.py)는
published_artifact 마커 뒤에 있어 CI 에서만 돈다.

처음에는 반대였다 — feed-data 는 사람이 쓰고, 안 따라온 것을 CI 가 알려주는
구조였다. 2026-09-09 부터 09-10 까지 발행본 아홉 중 다섯만 랜딩에 노출된
상태가 하루 지속된 것이 그 구조에서 일어났다. 뒤집은 이유는 언어다 — 랜딩이
셋(ko·ja·en)이 되면 사람이 유지할 줄이 세 배가 되고, #68·#72 로 세운 "새
피드는 자동으로 검사에 든다" 는 방향과도 어긋난다.

### 복수형은 조립기만 든다 — 문장은 복수형을 모른다

문구를 둘로 가른다.

**조립기**는 숫자와 단위가 붙어 한 조각이 되는 것이다(`count`·`days_ago`). 이쪽만
`one`·`other` 두 갈래를 드는 매핑이고, 고르는 것은 페이지 스크립트의 `fmt()` 다 —
`n === 1` 이면 `one`, 아니면 `other`. 갈래의 내용은 문구가 말하고 어느 갈래인지는
`fmt` 가 말한다.

**문장**은 복수형을 모른다. 이미 조립된 조각이나 숫자를 받을 뿐이고, 어느 자리에
무엇을 쓸지는 locale 이 정한다. 문장 쪽에 갈래를 들리지 않는 것은, 그러면 문장
하나가 갈래 수만큼 복제되고 문장이 길어질수록 번역자가 같은 문장을 두 번 고치게
되기 때문이다.

복수형이 없는 언어는 `one`·`other` 에 같은 값을 넣는다. 갈래가 없는 언어에서 한
줄로 적지 않는 것은, 그러면 조립기의 모양이 언어마다 달라져 "어느 키가 조립기인가"
를 언어별로 다시 읽어야 하기 때문이다.

**검사는 render 가 든다 — 테스트가 아니다.** 매핑이 `one`·`other` 를 정확히 들지
않으면(갈래가 빠졌거나 모르는 갈래가 있으면) `ValueError` 로 발행이 멈춘다. 층 1
경로다. 테스트에 맡기지 않은 근거는 실측이다 — 갈래 하나를 빼고 돌려 보면 render
는 돌고 테스트는 전부 녹색인데(랜딩 문구를 검사하는 테스트가 없다) 브라우저에서
`fmt` 가 `undefined` 에 걸려 그 블록이 실패로 넘어간다. CI 가 녹색인데 구독자는
숫자를 못 보는 꼴이고, 그것은 2026-09-09 노출 공백과 같은 형태다. 발행을 멈추는
쪽이 깨진 페이지를 내보내는 쪽보다 낫다.

모르는 갈래(`few`·`many`)도 막는다. 필요해지면 그때 연다 — 지금 통과시키면 오타가
생성물까지 간다.

원문 대조 절의 두 문구(`verify_intro`·`verify_stat`)는 숫자를 받지만 **단위를 붙이지
않아** 조립기를 쓰지 않는다. 무엇을 세는지는 앞 문장이 말하고, 숫자 자리는 render
가 `<span data-*>` 로 박아 스크립트가 숫자만 덮는다. 영어가 들어온 뒤에도 그대로였다.

그래서 **어느 키가 조립기인지는 아무것도 들고 있지 않다.** render 는 형태만 보고
"이 키는 매핑이어야 한다" 를 보지 않는다. 그 목록을 누가 들어야 하는지는 **단위가
붙는 문장이 생길 때** 열린다 — 그때 조립된 조각을 문장 자리에 넣어야 하고, 그러려면
span 치환 경로와 스크립트의 숫자 채우기를 함께 고쳐야 한다. 그 전에 목록을 만들면
쓰이지 않는 규약이 하나 더 생긴다.

### 주 이름 표기는 모듈이 둘을 들고 locale 이 고른다

독일 주 피드의 주 이름은 두 자리에 쓰인다 — 피드의 DESCRIPTION 첫 줄과 랜딩의
구독 줄(label·desc). 앞은 구독자의 캘린더에 들어가고 뒤는 페이지에 뜬다. 언어가
갈리는 것은 뒤쪽뿐이다.

그래서 모듈이 상수 둘을 든다. `LAND_NAME`(한국어)은 `SCOPE_SENTENCE` 를 거쳐
**피드에 나간다** — 바꾸면 전 구독자에게 개정으로 나가므로 언어축에서 제외한다.
`LAND_NAME_DE`(독일어)는 **피드에 나가지 않는다**. `landing/render.py` 만 읽는다.
둘을 한 상수로 합치지 않는 이유가 이 비대칭이다.

어느 표기를 쓸지는 locale 이 `state_feed.land_lang` 에 **닫힌 집합의 태그**로
적고 render 가 고른다. 집합 밖이면 `ValueError` — 허용값을 메시지에 적는다.
값은 언어 코드가 아니라 **표기 이름**이다. 그래서 render 안의 표는 언어 표가
아니라 표기 표이고, 언어가 늘어도 늘지 않는다 — "언어를 늘리는 것은 locales/ 에
파일 하나를 더하는 일" 이라는 위 결정을 지키기 위해서다. render 가 `lang` 을 보고
고르게 했다면 언어마다 코드를 고쳐야 한다.

label 도 같은 `{land}` 로 만든다. 전에는 `CALNAME` 에서 접미를 떼어 만들었는데,
`CALNAME` 이 한국어라 그 방식으로는 한국어 label 밖에 나오지 않았다. 언어별
`CALNAME` 을 만들지 않은 것은 의도다 — 주 16 개 × 언어 수로 곱해 늘고, 피드에
나가는 상수와 한 글자 차이로 나란히 서면 잘못 쓸 때 피드가 바뀐다.

주 이름의 출처는 **독일 기본법 전문(Grundgesetz, Präambel)** 이다. 기본법에서
16 개 주가 명사형으로 열거된 곳은 전문뿐이고, 한 조문이 16 개를 전부 덮으므로
2 차 배치 주가 들어와도 출처를 다시 찾지 않는다. 근거는 `rules/de/feed.py` 에
한 번만 적고 주 피드 아홉은 그 절을 가리킨다 — 같은 인용을 아홉 번 적으면
아홉 곳이 따로 낡는다.

### 표시 순서와 기계 순서는 일치시키지 않는다

랜딩 아코디언은 key 알파벳순이다. `status.json` 의 키는 `rules/status.py`
리터럴의 등록순이다. 앞은 사람이 훑는 순서고 뒤는 "언제 추가됐나" 를 말한다.
목적이 다르므로 맞추지 않고, 각각을 테스트가 따로 고정한다.

### 피드 목록의 단일 공급원은 publish.yml 의 FEEDS 다

`.github/workflows/publish.yml` 의 `FEEDS` 가 발행하는 목록이고, 생성 스텝과
커밋 스텝이 같은 목록을 돈다. 워크플로 주석이 이미 그렇게 선언하고 있다.
문서는 피드 목록을 다시 열거하지 않는다 — 열거하면 그 자리가 낡는다.
