"""일본 공휴일 소스 — 内閣府「国民の祝日について」CSV.

    cao_client   수집과 캐싱 (원본 바이트 보존, 조건부 GET)
    cao_parser   파싱과 검증 (응답 구조를 아는 유일한 곳)
    build_data   범위 안 데이터를 data/jp/ 로 옮긴다

규칙 유도(달력 조회·대체휴일 판정)는 여기 없다. rules/jp/ 가 생기면 그쪽이다.
"""
